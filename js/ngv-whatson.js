/**
 * A screenshow plugin to display whats on today information from the NGV public WhatsOn data feed:
 * http://www.ngv.vic.gov.au/xml-feeds/whats-on.xml
 */


/**
 * Callback which will be run by screenshow to initialise the plugin with the
 * cofig data as specified in the screenshow main config file
 */
function init(config) {

}

/**
 * Callback to
 * @returns {Array} An array of objects, each object represents 1 slide to be displayed in the
 * slide show. Each object is expected to have the following properties:
 *      duration {Number} the time in seconds for the slide
 *      html {String} the content of the slide
 */
function buildSlides() {

}